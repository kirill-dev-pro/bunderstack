# SQLite file, relative to this directory.
DATABASE_URL=file:./data.db

# Required when NODE_ENV=production. Dev falls back to a built-in secret.
# openssl rand -base64 32
AUTH_SECRET=

# Port for src/server.ts. Dev uses 3006 via the vite script.
PORT=3006
