{
  "name": "bunderstack-example-standalone",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot server.ts",
    "db:push": "drizzle-kit push --config=drizzle.config.ts"
  },
  "dependencies": {
    "bunderstack": "workspace:*"
  }
}
