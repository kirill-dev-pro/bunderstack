# Required: secret used to sign auth tokens (use a long random string in production)
AUTH_SECRET=change-me-before-production

# Database URL (SQLite). In Docker the compose file sets this automatically.
DATABASE_URL=file:./data.db

# Port to expose the app on (Docker only, default 3000)
PORT=3000
