{
  "name": "bunderstack-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "test": "bun test --cwd packages/bunderstack",
    "dev": "bun --hot examples/standalone/server.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
